成员的访问控制
向方法传递对象
从方法返回对象
重载方法
重载构造函数
使用递归
使用static关键字
使用内部类
使用可变长度实参

访问修饰符
    private package-privagte（在包中同public） protected public 
方法重载
    同一个类中两个或多个方法可以共享一个名称，只要形参声明不一样就可以了
    方法重载是java实现多态性的途径之一

    被重载的方法形参类型和数量必须不同。

    自动转换
        形参和实参类型不能直接匹配时
签名
    方法名及其形参列表
重载构造函数

递归
    方法调用自身
static关键字
    static 方法限制
        只能直接调用其他static 方法
        只能直接访问static 数据
        没法使用this
    static 代码快

快速排序
    选取分割值
    大于分割值的排左边，小于分割值的排右边
    继续分割直到排序完成


    选择的比较直最好位于中间，最坏情况位于末端
嵌套类和内部类
    嵌套类  java1.1
        作用域 外层类
        两种基本类型：
            带static 
            不带static 
                内部类
                    权限
                        访问外层类的所有变量和方法
                匿名内部类
                    没有名称
                    在声明该类时使用new初始化
                    15 介绍
                
            静态嵌套类与非静态嵌套类的区别
                健太嵌套类是使用static修饰符的类。因为是静态的，所以只能访问封装类中的其他静态成员。访问外部类成员时，必须通过对象引用来进行
可变长实参
    两种方式
        重载
        实参保存在数组中
    jdk5 varargs 
        ...
            告诉编译器，方法带有0个或多个实参
            把参数隐式声明为一个数组类型。
            方法内部通过数组方式使用变长参数

    重载 varargs
        支持重载
    歧义
        method1(int ...v )
        method1(boolean ...v) 
        psvn(){
            method1();
        }

        method(int ...v)
        method(int,int ...v)
        存在歧义
    








