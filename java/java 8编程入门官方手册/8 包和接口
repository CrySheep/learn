使用包
理解包如何影响访问
protected修饰符
导入包
了解java的标准包
理解接口的基础知识
实现接口
应用接口引用
了解接口变量
扩展接口
创建默认和静态接口方法


包：
    目的
        1 提供把相关程序段组织成一个单元的机制
        2 参与了java的访问控制机制
            包私有 
                只能被包里的访问
    默认包
    pacakge 
    目录结构和包名完全一致
    包和classpath 
        当前的工作目录（包含子目录）
        Classpath 环境变量
        javac 或java  中通过 -classpath 指定
    访问控制
        private 本类
        默认 本包
        protected 本包和子类
        public 所有
    导入包
        import
            全路径
            *
    java 类库中的包
        java.lang 通用类
        java.io     包含io类
        java.net    包含网络支持
        java.applet 包含创建applet的类
        java.awt 包含支持 abstract windows tookit 类

    接口
        interface 
        把接口和实现完全分开
        JDK8 接口默认方法
        接口方法
            默认 public 
        接口变量
            默认public static final
        实现接口
            implement
            实现多个
                , 分割
        接口扩展
            extends
    默认接口方法
        jdk8 
        允许为接口方法定义默认实现。
        也称为扩展方法
        主要作用：
            在不破坏现有代码的请扩，提供一种扩展接口的方式
            在无需类提供占位符实现的饿情况下，提供一种可选的功能
        关键字
            default
        多继承：
            类的实现优先于接口的默认实现
            某个类继承了两个具有默认方法的接口，并且该类没有重写默认方法，那将导致错误。
            接口继承另一个接口，并都定义了相同的默认方法。继承接口具有优先级。
    默认静态方法
        jdk8 
        定义一个或多个static 方法
        调用
            接口名称.方法
        


习题
    1 使用联系8-1中的代码，把ICharQ 接口和它的3中实现方式放进名为qpack 的包内。把演示队列的类IQDemo保存在默认包内，说明如何导入和使用qpack中的类
        答：要把ICharQ和它的实现放打扫qpack包中，必须把每一种实现放在单独的文件中，并且让每一种实现类都是公共的，然后把下面的语句添加到每一个文件的头部：package qpack; 完成上诉工作后，可以通过下面的 import语句添加到IQDemo中来使用 qpack ;  import qpack.*;
    2、什么是名称空间？为什么java允许区分名称空间很重要
        答：名称空间是一种声明的区域，通过却分名称空间，可以防止名称的冲突
    3、包存储在_________中
        答：目录
    4、解释受保护访问与默认访问方式不同。
        答：受保护方式的成员可以在其所在的包中使用，业可以由任何包的子类使用。默认访问方式的成员只能在其所在包中使用
    5、解释一个包的成员被其他包使用的两种方式
        可以全限定名称，或者使用import 导入它
    6、“一个接口，多个方法”是java的关键原则，什么特性可以最好体现这一点
        接口最好地体现了oop d的这一原则
    7、多少类可以实现一个接口？一个类可以实现多少个接口
        一个接口可以被任意多的类实现；一个类可以实现任意多的接口
    8、接口可以扩展吗
        可以
    9、为第7章中的Vehicle类创建一个接口。把该接口命名为IVehicle

    10、在接口中变量被隐式声明为static和final ，这样做有什么好处？
        接口变量的作用在于命名的常量可以由程序中所有的文件共享。通过导入变量所在的接口来使用接口变量
    11、包实际上是类的容器，这种说法正确还是错误？
        答： 正确
    12、什么标准java包是自动导入到程序中的。
        java.lang
    13、声明默认接口方法时使用的是哪个关键字
        default

    14、从jdk8 开始，可以在接口定义static 方法吗
        可以
    15、假定练习8-1中的ICharQ接口已广泛使用了多年。现在，向给它添加一个名为reset()的方法，该方法用于将队列重置为空队列即初始状态。jdk8或后续版本在不破坏先前存在的代码的情况向，如何实现这一点。
        使用默认接口方法
    16、如何调用接口中的static 方法
        接口名.方法