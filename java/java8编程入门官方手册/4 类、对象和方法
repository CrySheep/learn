类的基础知识
如何创建类
如何给引用变量赋值
创建方法，返回值，并且使用形参
return 
从方法返回值
构造函数
带形参的构造函数
new 运算符
来及回收和终止器
this关键字

如何创建对象
    Vehiele minivan = new Vehicle();

    声明一个名为minivan的Vehicle类型的变量。变量没有定义对象，只是一个引用
    声明通过new运算符创建了对象的一个实际副本，并把对象引用赋值给mnivan。

        new 运算符
            为对象动态分配内存，并返回一个对它的引用
方法
    ret-type name(paraeter-list){
        //body of method
    }

    形参 调用方法时接收传递到方法的值的变量
    实参 调用方法时实际传递到方法的变量

构造函数
    在创建对象时初始化对象。
    名称与类名相同
    没有显式的返回类型。
    可以带形参

finalize方法
    protected void finalize(){}

    在垃圾回收之前。
    对象不在作用域中时，是不会调用它。意味不知道何时执行finalize()，甚至finalize()是否会执行
this 关键字
    当调用一个方法，会自动传递一个隐式实参，它是对调用对象的一个引用